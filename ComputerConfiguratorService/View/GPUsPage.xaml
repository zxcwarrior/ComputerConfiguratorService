<Page x:Class="ComputerConfiguratorService.View.GPUsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ComputerConfiguratorService.View"
      mc:Ignorable="d" 
      Title="GPUsPage">

    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Редактировать" Style="{StaticResource ButtonStyle}" Width="150" Height="40" Click="EditButton_Click" Margin="5"/>
            <Button Content="Удалить" Style="{StaticResource ButtonStyle}" Width="150" Height="40" Click="DeleteButton_Click" Margin="5"/>
        </StackPanel>

        <ListView x:Name="LVGPUs" Grid.Row="1" Background="{x:Null}" SelectionMode="Single">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="#444444" BorderBrush="#555555" BorderThickness="2" CornerRadius="8" Margin="10" Padding="10">
                        <StackPanel Width="200">
                            <Image Source="{Binding ImagePath}" Height="150" Stretch="Uniform" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding Manufacturers.ManufacturerName, StringFormat='Производитель: {0}'}" Foreground="White" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding Vendors.VendorName, StringFormat='Вендор: {0}'}" Foreground="White" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding Model, StringFormat='Модель: {0}'}" Foreground="White" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding GPUMemoryTypes.MemoryType, StringFormat='Тип памяти: {0}'}" Foreground="White" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding MemoryGB, StringFormat='Объем: {0}GB'}" Foreground="White" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding CoreClock, StringFormat='Частота: {0}MHz'}" Foreground="White" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding GPULength, StringFormat='Длина: {0} мм'}" Foreground="White" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding PowerConsumption, StringFormat='Потребление: {0}W'}" Foreground="White" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding Price, StringFormat='Цена: {0}'}" Foreground="White" Margin="0,0,0,5"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border x:Name="EditPanel" Grid.Row="2" Background="#333333" Padding="10" Visibility="Collapsed" Margin="10">
            <StackPanel>
                <TextBlock Text="Редактирование/Добавление видеокарты" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Производитель:" Foreground="White" Width="120"/>
                    <ComboBox x:Name="cbManufacturer" Width="200" DisplayMemberPath="ManufacturerName" SelectedValuePath="ManufacturerID"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Вендор:" Foreground="White" Width="120"/>
                    <ComboBox x:Name="cbVendor" Width="200" DisplayMemberPath="VendorName" SelectedValuePath="VendorID"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Модель:" Foreground="White" Width="120"/>
                    <TextBox x:Name="tbModel" Width="200"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Тип памяти:" Foreground="White" Width="120"/>
                    <ComboBox x:Name="cbMemoryType" Width="200" DisplayMemberPath="MemoryType" SelectedValuePath="GPUMemoryTypeID"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Объем (GB):" Foreground="White" Width="120"/>
                    <TextBox x:Name="tbMemoryGB" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Частота (MHz):" Foreground="White" Width="120"/>
                    <TextBox x:Name="tbCoreClock" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Длина (мм):" Foreground="White" Width="120"/>
                    <TextBox x:Name="tbGPULength" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Потребление (W):" Foreground="White" Width="120"/>
                    <TextBox x:Name="tbPowerConsumption" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Цена:" Foreground="White" Width="120"/>
                    <TextBox x:Name="tbPrice" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="ImagePath:" Foreground="White" Width="120"/>
                    <TextBox x:Name="tbImagePath" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
                    <Button Content="Сохранить" Style="{StaticResource ButtonStyle}" Width="150" Height="40" Click="SaveButton_Click" Margin="5"/>
                    <Button Content="Отмена" Style="{StaticResource ButtonStyle}" Width="150" Height="40" Click="CancelButton_Click" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
