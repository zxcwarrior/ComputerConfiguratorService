<Page x:Class="ComputerConfiguratorService.View.ServiceMenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ComputerConfiguratorService.View"
      mc:Ignorable="d" 
      Title="ServiceMenuPage">
    <Grid Margin="10">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Меню сервиса" 
                       HorizontalAlignment="Center" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Margin="0,0,0,40"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                        Content="Комплектующие" 
                        Click="ComponentsButton_Click" 
                        Width="180" 
                        Height="50" 
                        Margin="0,0,10,0" 
                        FontSize="16" 
                        FontWeight="Bold"/>

                <!--<Button Grid.Column="2" 
                        Content="Сборки" 
                        Click="BuildsButton_Click" 
                        Width="180" 
                        Height="50" 
                        Margin="10,0,0,0" 
                        FontSize="16" 
                        FontWeight="Bold" Cursor="Hand"/>-->
                
                <Button Grid.Column="1" 
                        Content="Справочные данные" 
                        Click="BackDataButton_Click" 
                        Width="180" 
                        Height="50" 
                        Margin="10,0,0,0" 
                        FontSize="16" 
                        FontWeight="Bold"/>
            </Grid>
        </StackPanel>
    </Grid>
</Page>