<Page x:Class="ComputerConfiguratorService.View.ServiceMenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ComputerConfiguratorService.View"
      mc:Ignorable="d" 
      Title="ServiceMenuPage">

    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="Меню сервиса" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,40" HorizontalAlignment="Center"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Комплектующие" Style="{StaticResource ButtonStyle}" Click="ComponentsButton_Click" Margin="0,0,10,0" FontSize="20"/>

                <Button Grid.Column="1" Content="Сборки" Style="{StaticResource ButtonStyle}" Click="BuildsButton_Click" Margin="10,0,0,0" FontSize="20"/>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
